import React from 'react';
import './UserList.css';


const UserList = ({user}) => {
   
   return (
       <div className="block">
           
        {/* {user.map((user, index) => (
            <div key={index}>
                <h1 className="user">{user.name}</h1>
            </div>
           
        ))} */}
       </div>
   )
    
}

export default UserList; 
